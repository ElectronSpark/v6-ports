diff --git a/src/dispatch_common.c b/src/dispatch_common.c
index 3ba9b0f..c5c418b 100644
--- a/src/dispatch_common.c
+++ b/src/dispatch_common.c
@@ -160,12 +160,14 @@
 
 #include <assert.h>
 #include <stdlib.h>
+#include <stdint.h>
 #ifdef _WIN32
 #include <windows.h>
 #else
 #include <dlfcn.h>
 #include <err.h>
 #include <pthread.h>
+#include <unistd.h>
 #endif
 #include <string.h>
 #include <ctype.h>
@@ -277,6 +279,24 @@ static bool library_initialized;
 
 static bool epoxy_current_context_is_glx(void);
 
+static uintptr_t
+epoxy_xv6_missing_gl_stub(void)
+{
+    return 0;
+}
+
+void *
+epoxy_xv6_resolver_failure_stub(const char *name)
+{
+    if (!getenv("EPOXY_XV6_ALLOW_MISSING"))
+        return NULL;
+
+    write(2, "libepoxy: xv6 unresolved GL symbol ", 35);
+    write(2, name, strlen(name));
+    write(2, "\n", 1);
+    return (void *)epoxy_xv6_missing_gl_stub;
+}
+
 #if PLATFORM_HAS_EGL
 static EGLenum
 epoxy_egl_get_current_gl_context_api(void);
@@ -340,7 +360,17 @@ do_dlsym(void **handle, const char *name, bool exit_on_fail)
         error = dlerror();
 #endif
     if (!result && exit_on_fail) {
-        fprintf(stderr, "%s() not found: %s\n", name, error);
+        void *stub = epoxy_xv6_resolver_failure_stub(name);
+        if (stub)
+            return stub;
+
+        write(2, "libepoxy: missing symbol ", 25);
+        write(2, name, strlen(name));
+        write(2, ": ", 2);
+        if (error)
+            write(2, error, strlen(error));
+        write(2, "\n", 1);
+        fflush(stderr);
         abort();
     }
 
@@ -709,8 +739,14 @@ epoxy_gles1_dlsym(const char *name)
     if (epoxy_current_context_is_glx()) {
         return epoxy_get_proc_address(name);
     } else {
-        get_dlopen_handle(&api.gles1_handle, GLES1_LIB, true, true);
-        return do_dlsym(&api.gles1_handle, name, true);
+        if (get_dlopen_handle(&api.gles1_handle, GLES1_LIB, false, true)) {
+            void *func = do_dlsym(&api.gles1_handle, name, false);
+
+            if (func)
+                return func;
+        }
+
+        return epoxy_get_proc_address(name);
     }
 }
 
@@ -720,8 +756,14 @@ epoxy_gles2_dlsym(const char *name)
     if (epoxy_current_context_is_glx()) {
         return epoxy_get_proc_address(name);
     } else {
-        get_dlopen_handle(&api.gles2_handle, GLES2_LIB, true, true);
-        return do_dlsym(&api.gles2_handle, name, true);
+        if (get_dlopen_handle(&api.gles2_handle, GLES2_LIB, false, true)) {
+            void *func = do_dlsym(&api.gles2_handle, name, false);
+
+            if (func)
+                return func;
+        }
+
+        return epoxy_get_proc_address(name);
     }
 }
 
@@ -834,12 +876,14 @@ epoxy_get_bootstrap_proc_address(const char *name)
             return epoxy_gl_dlsym(name);
         case EGL_OPENGL_ES_API:
             if (eglQueryContext(eglGetCurrentDisplay(),
                                 eglGetCurrentContext(),
                                 EGL_CONTEXT_CLIENT_VERSION,
-                                &version)) {
-                if (version >= 2)
-                    return epoxy_gles2_dlsym(name);
-                else
-                    return epoxy_gles1_dlsym(name);
+                                &version) == EGL_FALSE) {
+                (void)eglGetError();
+                version = 2;
             }
+            if (version >= 2)
+                return epoxy_gles2_dlsym(name);
+            else
+                return epoxy_gles1_dlsym(name);
         }
     }
 #endif /* PLATFORM_HAS_EGL */
 
diff --git a/src/dispatch_common.h b/src/dispatch_common.h
index 55388a8..e32c056 100644
--- a/src/dispatch_common.h
+++ b/src/dispatch_common.h
@@ -186,6 +186,7 @@ extern void UNWRAPPED_PROTO(glBegin_unwrapped)(GLenum primtype);
 extern void UNWRAPPED_PROTO(glEnd_unwrapped)(void);
 
 extern epoxy_resolver_failure_handler_t epoxy_resolver_failure_handler;
+void *epoxy_xv6_resolver_failure_stub(const char *name);
 
 #if USING_DISPATCH_TABLE
 void gl_init_dispatch_table(void);
diff --git a/src/gen_dispatch.py b/src/gen_dispatch.py
index 3daad84..a37b166 100755
--- a/src/gen_dispatch.py
+++ b/src/gen_dispatch.py
@@ -730,6 +730,12 @@ class Generator(object):
         self.outln('    if (epoxy_resolver_failure_handler)')
         self.outln('        return epoxy_resolver_failure_handler(name);')
         self.outln('')
+        self.outln('    {')
+        self.outln('        void *stub = epoxy_xv6_resolver_failure_stub(name);')
+        self.outln('        if (stub)')
+        self.outln('            return stub;')
+        self.outln('    }')
+        self.outln('')
 
         # If the function isn't provided by any known extension, print
         # something useful for the poor application developer before
